// Pagination
export const PAGE_SIZE_LIST = 30; // supplier admin lists (orders, invoices, products, etc.)
export const PAGE_SIZE_CATALOG = 10; // buyer product catalog (set lower for UI testing; raise to 50 for production)

// Invitation tokens
export const INVITATION_EXPIRY_MS = 24 * 60 * 60 * 1000; // 24 hours

// Invoice numbering
export const INVOICE_NUMBER_PREFIX = 'INV';
export const INVOICE_NUMBER_DIGITS = 4;
